# 📅 Day 32: Pod 상세 학습

## 🎯 오늘의 목표

> **토스플레이스 연결점**: "Kubernetes 클러스터를 운영/최적화"
> Pod은 Kubernetes의 가장 기본이 되는 단위로, 이를 완벽히 이해해야 함

Pod의 구조, 생명주기, YAML 정의 방법을 깊이 있게 학습하고 다양한 패턴을 실습합니다.

---

## ⏰ 예상 학습 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| Pod 개념 | 45분 | 구조, 특징, 생명주기 |
| 기본 실습 | 1시간 | YAML 작성, 명령어 |
| 멀티 컨테이너 | 1시간 | Sidecar, Ambassador 패턴 |
| 고급 설정 | 1시간 | 리소스, Probe, 환경변수 |
| 정리 | 15분 | 복습 및 체크리스트 |

---

## 📚 Part 1: Pod 개념 (45분)

### 1.1 Pod이란?

```
┌─────────────────────────────────────────────────────────────────────┐
│  Pod = Kubernetes의 최소 배포 단위                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  특징:                                                               │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  • 하나 이상의 컨테이너 그룹                                │    │
│  │  • 같은 네트워크 네임스페이스 공유 (같은 IP)                │    │
│  │  • 스토리지 볼륨 공유 가능                                  │    │
│  │  • 함께 스케줄링됨 (같은 노드)                              │    │
│  │  • 함께 생성되고 함께 삭제됨                                │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  왜 Pod인가?:                                                        │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  Container만으로 부족한 이유:                               │    │
│  │  • 밀접하게 협력하는 컨테이너들을 묶어야 함                │    │
│  │  • 예: App + Log Agent, App + Sidecar Proxy               │    │
│  │  • 이들이 네트워크/스토리지를 쉽게 공유해야 함             │    │
│  │  → Pod이 이 문제를 해결!                                   │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 Pod 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│  Pod 내부 구조                                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Pod (IP: 10.244.0.5)                                         │  │
│  │  ┌─────────────────┐  ┌─────────────────┐                    │  │
│  │  │ Container 1     │  │ Container 2     │                    │  │
│  │  │ (app)           │  │ (sidecar)       │                    │  │
│  │  │ Port: 8080      │  │ Port: 15001     │                    │  │
│  │  └────────┬────────┘  └────────┬────────┘                    │  │
│  │           │                    │                              │  │
│  │           └───────┬────────────┘                              │  │
│  │                   │                                           │  │
│  │           ┌───────▼───────┐                                   │  │
│  │           │ Shared Volume │                                   │  │
│  │           │ (emptyDir)    │                                   │  │
│  │           └───────────────┘                                   │  │
│  │                                                               │  │
│  │  네트워크:                                                    │  │
│  │  • 컨테이너 간 localhost로 통신                              │  │
│  │  • Container 1 → localhost:15001 → Container 2               │  │
│  │  • 외부에서 → 10.244.0.5:8080 → Container 1                 │  │
│  │                                                               │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  Pause 컨테이너 (인프라 컨테이너):                                  │
│  • 모든 Pod에 존재하는 숨겨진 컨테이너                              │
│  • 네트워크 네임스페이스 유지                                       │
│  • 다른 컨테이너가 재시작되어도 Pod IP 유지                         │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.3 Pod 생명주기

```
┌─────────────────────────────────────────────────────────────────────┐
│  Pod 생명주기 (Lifecycle)                                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  상태 (Phase):                                                       │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                                                              │    │
│  │  Pending ──────> Running ──────> Succeeded/Failed           │    │
│  │     │               │                                        │    │
│  │     │               │                                        │    │
│  │     │               ▼                                        │    │
│  │     │           CrashLoopBackOff (반복 실패)                │    │
│  │     │                                                        │    │
│  │     ▼                                                        │    │
│  │  ImagePullBackOff (이미지 문제)                             │    │
│  │                                                              │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  상태 상세:                                                          │
│  • Pending: Pod이 승인됨, 아직 실행 안 됨 (스케줄링/이미지 풀)     │
│  • Running: Pod이 노드에 바인딩, 모든 컨테이너 생성됨               │
│  • Succeeded: 모든 컨테이너가 성공적으로 종료 (재시작 안 함)        │
│  • Failed: 모든 컨테이너 종료, 최소 하나가 실패로 종료              │
│  • Unknown: Pod 상태를 알 수 없음 (노드 통신 실패)                  │
│                                                                      │
│  일반적인 문제 상태:                                                 │
│  • ImagePullBackOff: 이미지를 가져올 수 없음                        │
│  • CrashLoopBackOff: 컨테이너가 반복적으로 크래시                   │
│  • Evicted: 노드 리소스 부족으로 퇴출                               │
│  • OOMKilled: 메모리 부족으로 종료                                  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.4 Pod vs Container vs Deployment

| 개념 | 설명 | 생성 방법 | 자가 치유 |
|------|------|----------|----------|
| Container | 단일 프로세스 격리 단위 | Docker, containerd | ❌ |
| Pod | 컨테이너 그룹, 공유 네트워크/스토리지 | kubectl run, YAML | ❌ |
| Deployment | Pod 템플릿 + 복제본 관리 | YAML | ✅ |

```
⚠️ 실무에서는 Pod을 직접 생성하지 않음!
→ Deployment를 통해 Pod을 관리
→ 자가 치유, 롤링 업데이트 등 제공
→ Day 33에서 Deployment 학습
```

---

## 🛠️ Part 2: 기본 실습 (1시간)

### 실습 1: YAML로 Pod 생성 (30분)

```bash
# 작업 디렉토리 생성
mkdir -p ~/k8s-practice/day32
cd ~/k8s-practice/day32

# Pod YAML 작성
cat << 'EOF' > nginx-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
    environment: development
    version: v1
spec:
  containers:
  - name: nginx
    image: nginx:1.24
    ports:
    - containerPort: 80
      name: http
    - containerPort: 443
      name: https
EOF

# Pod 생성
kubectl apply -f nginx-pod.yaml

# 생성 확인
kubectl get pods
kubectl get pods -o wide  # 더 많은 정보 (Node, IP)
```

**YAML 구조 상세 설명**:
```yaml
apiVersion: v1           # API 버전 (Pod은 core/v1)
kind: Pod                # 리소스 종류
metadata:                # 메타데이터 (이름, 레이블 등)
  name: nginx-pod        # Pod 이름 (필수, 고유해야 함)
  labels:                # 레이블 (선택, 선택자로 사용)
    app: nginx
spec:                    # Pod 스펙 (원하는 상태)
  containers:            # 컨테이너 목록 (1개 이상)
  - name: nginx          # 컨테이너 이름 (Pod 내 고유)
    image: nginx:1.24    # 컨테이너 이미지
    ports:               # 노출할 포트 (문서화 목적)
    - containerPort: 80
```

### 실습 2: Pod 상세 정보 확인 (15분)

```bash
# Pod 상세 정보
kubectl describe pod nginx-pod

# 출력에서 확인할 주요 정보:
# - Name, Namespace, Node, IP
# - Status (Running, Pending 등)
# - Containers 섹션 (이미지, 포트, 상태)
# - Events 섹션 (최근 이벤트)

# YAML 형식으로 전체 정보
kubectl get pod nginx-pod -o yaml

# JSON 형식
kubectl get pod nginx-pod -o json

# 특정 필드만 추출
kubectl get pod nginx-pod -o jsonpath='{.status.podIP}'
kubectl get pod nginx-pod -o jsonpath='{.spec.containers[0].image}'
```

### 실습 3: Pod 접속 및 테스트 (15분)

```bash
# Pod 내부 접속 (bash)
kubectl exec -it nginx-pod -- /bin/bash

# 내부에서 확인
hostname
cat /etc/nginx/nginx.conf
curl localhost
exit

# 단일 명령 실행
kubectl exec nginx-pod -- nginx -v
kubectl exec nginx-pod -- ls /usr/share/nginx/html

# 포트 포워딩 (로컬에서 접근)
kubectl port-forward nginx-pod 8080:80 &
curl http://localhost:8080

# 포트 포워딩 종료
pkill -f "port-forward"
```

---

## 🛠️ Part 3: 멀티 컨테이너 Pod (1시간)

### 3.1 멀티 컨테이너 패턴

```
┌─────────────────────────────────────────────────────────────────────┐
│  멀티 컨테이너 패턴                                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  1. Sidecar Pattern                                                  │
│  ┌──────────────────────────────────────────────┐                   │
│  │  Main Container        Sidecar Container     │                   │
│  │  (애플리케이션)   +    (로그 수집, 프록시)   │                   │
│  └──────────────────────────────────────────────┘                   │
│  예: Istio Envoy Proxy, Fluentd Log Agent                          │
│                                                                      │
│  2. Ambassador Pattern                                               │
│  ┌──────────────────────────────────────────────┐                   │
│  │  Main Container        Ambassador Container  │                   │
│  │  (애플리케이션)   →   (외부 연결 대행)       │                   │
│  └──────────────────────────────────────────────┘                   │
│  예: DB 연결 프록시, API Gateway                                    │
│                                                                      │
│  3. Adapter Pattern                                                  │
│  ┌──────────────────────────────────────────────┐                   │
│  │  Main Container        Adapter Container     │                   │
│  │  (레거시 앱)      +    (포맷 변환)          │                   │
│  └──────────────────────────────────────────────┘                   │
│  예: 로그 포맷 변환, 메트릭 변환 (Prometheus exporter)             │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 실습 4: Sidecar 패턴 - 볼륨 공유 (30분)

```bash
cat << 'EOF' > sidecar-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: sidecar-pod
  labels:
    app: sidecar-demo
spec:
  containers:
  # 메인 컨테이너: Nginx (웹 서버)
  - name: nginx
    image: nginx:1.24
    ports:
    - containerPort: 80
    volumeMounts:
    - name: shared-data
      mountPath: /usr/share/nginx/html
  
  # Sidecar 컨테이너: 콘텐츠 생성기
  - name: content-creator
    image: alpine
    command: ["/bin/sh", "-c"]
    args:
      - |
        while true; do
          echo "<!DOCTYPE html>
          <html>
          <body>
          <h1>Generated at $(date)</h1>
          <p>Pod: $(hostname)</p>
          </body>
          </html>" > /data/index.html
          sleep 10
        done
    volumeMounts:
    - name: shared-data
      mountPath: /data

  # 공유 볼륨 정의
  volumes:
  - name: shared-data
    emptyDir: {}
EOF

kubectl apply -f sidecar-pod.yaml

# 상태 확인
kubectl get pod sidecar-pod

# 각 컨테이너 접속
kubectl exec -it sidecar-pod -c nginx -- cat /usr/share/nginx/html/index.html
kubectl exec -it sidecar-pod -c content-creator -- cat /data/index.html

# 포트 포워딩으로 확인
kubectl port-forward sidecar-pod 8081:80 &
curl http://localhost:8081
sleep 15
curl http://localhost:8081  # 내용이 변경됨!

pkill -f "port-forward"
```

### 실습 5: 로그 수집 Sidecar (30분)

```bash
cat << 'EOF' > log-sidecar-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: log-sidecar-pod
  labels:
    app: log-demo
spec:
  containers:
  # 메인 앱: 로그 생성
  - name: app
    image: busybox
    command: ["/bin/sh", "-c"]
    args:
      - |
        while true; do
          echo "$(date) - App log message: request_id=$(cat /dev/urandom | tr -dc 'a-z0-9' | head -c 8)" >> /var/log/app.log
          sleep 5
        done
    volumeMounts:
    - name: log-volume
      mountPath: /var/log
  
  # Sidecar: 로그 읽기 및 처리
  - name: log-reader
    image: busybox
    command: ["/bin/sh", "-c"]
    args:
      - |
        tail -f /var/log/app.log | while read line; do
          echo "[LOG-READER] $line"
        done
    volumeMounts:
    - name: log-volume
      mountPath: /var/log
      readOnly: true

  volumes:
  - name: log-volume
    emptyDir: {}
EOF

kubectl apply -f log-sidecar-pod.yaml

# 로그 확인 (각 컨테이너별)
kubectl logs log-sidecar-pod -c app --tail=5
kubectl logs log-sidecar-pod -c log-reader --tail=5

# 실시간 로그 (log-reader가 처리한 로그)
kubectl logs log-sidecar-pod -c log-reader -f
# Ctrl+C로 종료
```

---

## 🛠️ Part 4: 고급 설정 (1시간)

### 실습 6: 리소스 제한 (20분)

```bash
cat << 'EOF' > resource-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: resource-pod
  labels:
    app: resource-demo
spec:
  containers:
  - name: app
    image: nginx
    resources:
      # 최소 보장 리소스 (스케줄링 기준)
      requests:
        memory: "64Mi"
        cpu: "100m"      # 100 밀리코어 = 0.1 CPU
      # 최대 허용 리소스 (초과 시 제한/종료)
      limits:
        memory: "128Mi"
        cpu: "200m"      # 200 밀리코어 = 0.2 CPU
EOF

kubectl apply -f resource-pod.yaml

# 리소스 설정 확인
kubectl describe pod resource-pod | grep -A 10 "Limits\|Requests"

# 실제 리소스 사용량 확인 (metrics-server 필요)
kubectl top pod resource-pod
```

**리소스 단위 설명**:
```
┌─────────────────────────────────────────────────────────────────────┐
│  리소스 단위                                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  CPU:                                                                │
│  • 1 = 1 vCPU/Core                                                  │
│  • 1000m (밀리코어) = 1 CPU                                         │
│  • 100m = 0.1 CPU = 10% CPU                                         │
│                                                                      │
│  Memory:                                                             │
│  • Ki (키비바이트) = 1024 bytes                                     │
│  • Mi (메비바이트) = 1024 Ki                                        │
│  • Gi (기비바이트) = 1024 Mi                                        │
│  • 예: 128Mi = 약 134MB                                             │
│                                                                      │
│  requests vs limits:                                                 │
│  • requests: 스케줄링 보장량 (노드 선택 기준)                       │
│  • limits: 최대 사용량 (초과 시 CPU throttle, Memory OOMKill)       │
│                                                                      │
│  ⚠️ 권장:                                                           │
│  • requests = 평상시 사용량                                         │
│  • limits = 피크 사용량 (보통 requests의 1.5~2배)                   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 실습 7: 환경 변수 (15분)

```bash
cat << 'EOF' > env-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: env-pod
spec:
  containers:
  - name: app
    image: busybox
    command: ["/bin/sh", "-c", "env && sleep 3600"]
    env:
    # 직접 값 지정
    - name: APP_NAME
      value: "my-application"
    - name: LOG_LEVEL
      value: "INFO"
    
    # Pod 정보 주입 (Downward API)
    - name: POD_NAME
      valueFrom:
        fieldRef:
          fieldPath: metadata.name
    - name: POD_NAMESPACE
      valueFrom:
        fieldRef:
          fieldPath: metadata.namespace
    - name: POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
    - name: NODE_NAME
      valueFrom:
        fieldRef:
          fieldPath: spec.nodeName
EOF

kubectl apply -f env-pod.yaml

# 환경 변수 확인
kubectl exec env-pod -- env | grep -E "APP_|LOG_|POD_|NODE_"
```

### 실습 8: Probe 설정 (25분)

```bash
cat << 'EOF' > probe-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: probe-pod
  labels:
    app: probe-demo
spec:
  containers:
  - name: nginx
    image: nginx:1.24
    ports:
    - containerPort: 80
    
    # Liveness Probe: 컨테이너가 살아있는지 확인
    # 실패 시 → 컨테이너 재시작
    livenessProbe:
      httpGet:
        path: /
        port: 80
      initialDelaySeconds: 5    # 첫 체크까지 대기
      periodSeconds: 10         # 체크 간격
      timeoutSeconds: 3         # 응답 대기 시간
      failureThreshold: 3       # 연속 실패 허용 횟수
    
    # Readiness Probe: 트래픽 받을 준비가 됐는지 확인
    # 실패 시 → Service에서 제외 (트래픽 안 받음)
    readinessProbe:
      httpGet:
        path: /
        port: 80
      initialDelaySeconds: 3
      periodSeconds: 5
      failureThreshold: 3
    
    # Startup Probe: 앱이 시작됐는지 확인 (느린 앱용)
    # 성공 전까지 다른 Probe 비활성화
    startupProbe:
      httpGet:
        path: /
        port: 80
      initialDelaySeconds: 0
      periodSeconds: 5
      failureThreshold: 30      # 30*5=150초까지 시작 대기
EOF

kubectl apply -f probe-pod.yaml

# Probe 상태 확인
kubectl describe pod probe-pod | grep -A 15 "Liveness\|Readiness\|Startup"

# Pod Ready 상태 확인
kubectl get pod probe-pod -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'
```

**Probe 종류와 용도**:
```
┌─────────────────────────────────────────────────────────────────────┐
│  Kubernetes Probes                                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Liveness Probe (생존 검사)                                         │
│  ├── 목적: 컨테이너가 살아있는지 확인                              │
│  ├── 실패 시: 컨테이너 재시작                                       │
│  └── 사용: 앱이 데드락 상태일 때 복구                              │
│                                                                      │
│  Readiness Probe (준비 검사)                                        │
│  ├── 목적: 트래픽을 처리할 준비가 됐는지 확인                      │
│  ├── 실패 시: Service 엔드포인트에서 제거 (트래픽 차단)            │
│  └── 사용: 시작 중, 과부하 시 트래픽 차단                          │
│                                                                      │
│  Startup Probe (시작 검사)                                          │
│  ├── 목적: 앱 시작 완료 확인                                       │
│  ├── 실패 시: 컨테이너 재시작                                       │
│  └── 사용: 시작이 오래 걸리는 앱 (레거시, 대용량 캐시 로드)        │
│                                                                      │
│  Probe 방법:                                                         │
│  • httpGet: HTTP 요청 (200~399 성공)                               │
│  • tcpSocket: TCP 연결 시도                                         │
│  • exec: 컨테이너 내 명령 실행 (exit 0 성공)                       │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Part 5: Pod 문제 해결 (선택)

### 자주 발생하는 Pod 문제

| 상태 | 원인 | 확인 방법 | 해결 |
|------|------|----------|------|
| `Pending` | 노드 리소스 부족 | `describe pod` → Events | 리소스 조정, 노드 추가 |
| `ImagePullBackOff` | 이미지 없음/권한 | `describe pod` | 이미지 이름, Secret 확인 |
| `CrashLoopBackOff` | 앱 오류 | `logs` | 코드/설정 수정 |
| `OOMKilled` | 메모리 부족 | `describe pod` | limits 증가 |
| `Evicted` | 노드 리소스 압박 | `describe pod` | 노드 리소스 확보 |

```bash
# 문제 진단 명령어 모음
kubectl get pods                           # 전체 상태
kubectl describe pod <name>                # 상세 정보 + Events
kubectl logs <name>                        # 로그
kubectl logs <name> --previous             # 이전 컨테이너 로그
kubectl get events --sort-by='.lastTimestamp'  # 최근 이벤트
```

---

## ✅ 오늘의 체크리스트

| # | 항목 | 설명 | 완료 |
|---|------|------|------|
| 1 | Pod 개념 이해 | 구조, 특징 | ☐ |
| 2 | YAML로 Pod 생성 | apiVersion, kind, metadata, spec | ☐ |
| 3 | kubectl exec 사용 | Pod 내부 접속 | ☐ |
| 4 | 멀티 컨테이너 Pod | Sidecar 패턴 | ☐ |
| 5 | 볼륨 공유 | emptyDir | ☐ |
| 6 | 리소스 제한 | requests, limits | ☐ |
| 7 | 환경 변수 | env, Downward API | ☐ |
| 8 | Probe 설정 | liveness, readiness, startup | ☐ |

---

## 🔑 오늘 배운 핵심 명령어

```bash
# Pod 생성/삭제
kubectl apply -f <yaml>
kubectl delete -f <yaml>
kubectl delete pod <name>

# Pod 확인
kubectl get pods
kubectl get pods -o wide
kubectl describe pod <name>

# Pod 접속
kubectl exec -it <pod> -- /bin/bash
kubectl exec -it <pod> -c <container> -- sh

# 로그
kubectl logs <pod>
kubectl logs <pod> -c <container>
kubectl logs <pod> -f              # 실시간
kubectl logs <pod> --previous      # 이전 컨테이너

# 포트 포워딩
kubectl port-forward <pod> 8080:80
```

---

## 💡 면접 대비 핵심 포인트

### Q1: Pod과 Container의 차이는?

**A**: "Container는 단일 프로세스 격리 단위이고, Pod은 하나 이상의 컨테이너를 묶는 Kubernetes의 최소 배포 단위입니다. Pod 내 컨테이너들은 같은 IP를 공유하고 localhost로 통신하며, 볼륨도 공유할 수 있습니다."

### Q2: 왜 Pod을 직접 생성하지 않고 Deployment를 사용하나요?

**A**: "Pod은 자가 치유 기능이 없습니다. Pod이 죽으면 끝입니다. Deployment는 desired state를 유지하여 Pod이 죽으면 자동으로 새 Pod을 생성하고, 롤링 업데이트, 롤백 등의 기능도 제공합니다."

### Q3: Liveness Probe와 Readiness Probe의 차이는?

**A**: "Liveness는 컨테이너가 살아있는지 확인하고 실패 시 재시작합니다. Readiness는 트래픽을 받을 준비가 됐는지 확인하고 실패 시 Service에서 제외합니다. Liveness 실패는 컨테이너를 죽이고, Readiness 실패는 트래픽만 차단합니다."

---

## 📝 학습 기록

```
학습일: ____년 __월 __일
실제 소요 시간: ____시간

완료한 실습:
- [ ] nginx-pod.yaml
- [ ] sidecar-pod.yaml
- [ ] log-sidecar-pod.yaml
- [ ] resource-pod.yaml
- [ ] env-pod.yaml
- [ ] probe-pod.yaml

이해가 어려웠던 부분:


추가 학습 필요 항목:

```

---

## 🧹 정리

```bash
# 실습 리소스 삭제
kubectl delete pod nginx-pod sidecar-pod log-sidecar-pod resource-pod env-pod probe-pod

# 확인
kubectl get pods
```

---

## ➡️ 다음 학습: Day 33

**주제**: Deployment 학습
- ReplicaSet과 Deployment
- 롤링 업데이트
- 롤백
- 스케일링
