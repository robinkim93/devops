# 📅 Day 112: Month 4 프로젝트 - 설계

## 🎯 포트폴리오 프로젝트 #4

> **GitOps CI/CD 파이프라인 구축**: GoCD + ArgoCD + Vault로 완전한 GitOps 파이프라인을 구축합니다.

---

## ⏰ 예상 소요 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| 아키텍처 설계 | 1시간 30분 | 전체 흐름 설계 |
| 레포지토리 구조 | 1시간 | 디렉토리 설계 |
| 환경 설정 | 1시간 30분 | 개발 환경 준비 |

---

## 📚 Part 1: 프로젝트 아키텍처

### 전체 아키텍처

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                     GitOps CI/CD Pipeline Project                            │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        CI: GoCD Pipeline                             │   │
│   │                                                                      │   │
│   │   [Git Push] → [Build] → [Test] → [Push Image] → [Update Manifest]  │   │
│   │                                                                      │   │
│   │   구현:                                                              │   │
│   │   - Pipeline as Code (gocd.yaml)                                    │   │
│   │   - Elastic Kubernetes Agent                                        │   │
│   │   - Docker 이미지 빌드/푸시                                          │   │
│   │   - 보안 스캔 (Trivy)                                               │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                              │                                               │
│                              ▼ Update image tag                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        Infra Repository                              │   │
│   │                                                                      │   │
│   │   base/            # 공통 K8s 매니페스트                             │   │
│   │   overlays/        # 환경별 설정 (dev, staging, prod)               │   │
│   │   argocd/          # ArgoCD Application 정의                        │   │
│   │   vault/           # Vault 설정                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                              │                                               │
│                              ▼ Watch changes                                 │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        CD: ArgoCD                                    │   │
│   │                                                                      │   │
│   │   - Auto Sync                                                        │   │
│   │   - Self-Heal                                                        │   │
│   │   - App of Apps 패턴                                                 │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                              │                                               │
│                              ▼ Deploy                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        Kubernetes + Vault                            │   │
│   │                                                                      │   │
│   │   ExternalSecret → SecretStore → Vault                              │   │
│   │   Argo Rollouts → 카나리 배포                                        │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 구현 목표

| 컴포넌트 | 구현 내용 | 토스플레이스 연관 |
|---------|----------|------------------|
| **GoCD** | Pipeline as Code, K8s Agent | GoCD 이용 경험 |
| **ArgoCD** | GitOps, Auto Sync | ArgoCD GitOps |
| **Vault** | 시크릿 저장, K8s Auth | Vault 시크릿 관리 |
| **ESO** | 시크릿 동기화 | Vault + K8s |
| **Argo Rollouts** | 카나리 배포 | 안전한 배포 |
| **Kustomize** | 환경 분리 | 환경 관리 |

---

## 📚 Part 2: 프로젝트 일정

| Day | 작업 | 상세 내용 |
|-----|------|----------|
| 112 | **설계** | 아키텍처, 레포지토리 구조 |
| 113 | **GoCD** | CI 파이프라인 구축 |
| 114 | **ArgoCD** | CD 설정, Application 생성 |
| 115 | **Vault/ESO** | 시크릿 관리 연동 |
| 116 | **Kustomize** | 환경별 설정 분리 |
| 117 | **Argo Rollouts** | 카나리 배포 구현 |
| 118 | **문서화** | README, 아키텍처 문서 |
| 119 | **테스트** | E2E 테스트, 검증 |
| 120 | **완료** | GitHub 업로드, 정리 |

---

## 📚 Part 3: 레포지토리 구조

### App Repository (애플리케이션 코드)

```
app-repo/
├── cmd/
│   └── main.go
├── internal/
│   ├── handlers/
│   └── service/
├── Dockerfile
├── Makefile
├── go.mod
├── go.sum
├── gocd.yaml                 # GoCD Pipeline 정의
└── README.md
```

### Infra Repository (K8s 매니페스트)

```
infra-repo/
├── apps/                     # ArgoCD Applications
│   ├── myapp-dev.yaml
│   ├── myapp-staging.yaml
│   ├── myapp-prod.yaml
│   └── root-app.yaml         # App of Apps
│
├── base/                     # Kustomize Base
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── serviceaccount.yaml
│   ├── external-secret.yaml
│   └── kustomization.yaml
│
├── overlays/                 # 환경별 Overlay
│   ├── dev/
│   │   ├── kustomization.yaml
│   │   └── patches/
│   ├── staging/
│   │   ├── kustomization.yaml
│   │   └── patches/
│   └── prod/
│       ├── kustomization.yaml
│       ├── rollout.yaml      # Argo Rollouts
│       └── patches/
│
├── vault/                    # Vault 설정
│   ├── policy.hcl
│   └── setup.sh
│
├── gocd/                     # GoCD 설정
│   ├── elastic-profiles.yaml
│   └── environments.yaml
│
└── README.md
```

---

## 🛠️ Part 4: 환경 준비

### 사전 요구사항

```bash
# Kubernetes 클러스터 (EKS 또는 Kind)
kubectl version

# Helm 설치
helm version

# GoCD CLI (선택)
brew install gocd

# ArgoCD CLI
brew install argocd

# istioctl (카나리 배포용, 선택)
brew install istioctl
```

### Kind 클러스터 생성 (로컬 테스트용)

```yaml
# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
```

```bash
kind create cluster --config kind-config.yaml --name gitops-demo
kubectl cluster-info
```

### 네임스페이스 생성

```bash
kubectl create namespace dev
kubectl create namespace staging
kubectl create namespace production
kubectl create namespace gocd
kubectl create namespace argocd
kubectl create namespace vault
kubectl create namespace external-secrets
kubectl create namespace argo-rollouts
```

---

## 🛠️ Part 5: 샘플 애플리케이션

### main.go

```go
package main

import (
	"encoding/json"
	"log"
	"net/http"
	"os"
)

type Response struct {
	Message string `json:"message"`
	Version string `json:"version"`
	Env     string `json:"env"`
}

func main() {
	http.HandleFunc("/health", func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusOK)
		w.Write([]byte("OK"))
	})

	http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
		resp := Response{
			Message: "Hello from GitOps Pipeline!",
			Version: os.Getenv("APP_VERSION"),
			Env:     os.Getenv("ENVIRONMENT"),
		}
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(resp)
	})

	log.Println("Server starting on :8080")
	log.Fatal(http.ListenAndServe(":8080", nil))
}
```

### Dockerfile

```dockerfile
# Build stage
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/main.go

# Runtime stage
FROM alpine:3.18
RUN adduser -D -g '' appuser
COPY --from=builder /app/server /server
USER appuser
EXPOSE 8080
CMD ["/server"]
```

### Makefile

```makefile
.PHONY: build test lint

VERSION ?= $(shell git rev-parse --short HEAD)
REGISTRY ?= myregistry

build:
	docker build -t $(REGISTRY)/myapp:$(VERSION) .

test:
	go test -v ./...

lint:
	golangci-lint run

push: build
	docker push $(REGISTRY)/myapp:$(VERSION)
```

---

## ✅ 오늘의 체크리스트

| # | 항목 | 완료 |
|---|------|------|
| 1 | 아키텍처 다이어그램 작성 | ☐ |
| 2 | 프로젝트 일정 수립 | ☐ |
| 3 | App Repository 생성 | ☐ |
| 4 | Infra Repository 생성 | ☐ |
| 5 | 샘플 애플리케이션 작성 | ☐ |
| 6 | Dockerfile 작성 | ☐ |
| 7 | K8s 클러스터 준비 | ☐ |
| 8 | 네임스페이스 생성 | ☐ |

---

## ➡️ 다음 학습: Day 113

**주제**: GoCD CI 파이프라인 구축

