# 📅 Day 115: Vault/ESO 시크릿 관리 연동

## 🎯 오늘의 목표

> Vault와 External Secrets Operator를 연동하여 시크릿을 안전하게 관리합니다.

---

## ⏰ 예상 소요 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| Vault 설치 | 1시간 | Dev 모드 또는 HA |
| ESO 설치 | 30분 | Helm 설치 |
| 연동 설정 | 1시간 30분 | SecretStore, Policy |
| 테스트 | 1시간 | ExternalSecret 동기화 |

---

## 🛠️ Part 1: Vault 설치

### Dev 모드 (테스트용)

```bash
helm repo add hashicorp https://helm.releases.hashicorp.com
helm install vault hashicorp/vault \
  -n vault \
  --set server.dev.enabled=true \
  --set server.dev.devRootToken="root"
```

### 시크릿 저장

```bash
# Vault 접속
kubectl exec -it -n vault vault-0 -- /bin/sh

# KV 엔진 활성화 (이미 활성화됨)
vault secrets enable -path=secret kv-v2

# 시크릿 저장
vault kv put secret/myapp/staging \
  db_host="staging-db.example.com" \
  db_password="staging-secret" \
  api_key="staging-api-key"

vault kv put secret/myapp/prod \
  db_host="prod-db.example.com" \
  db_password="prod-super-secret" \
  api_key="prod-api-key"
```

### K8s Auth 설정

```bash
# K8s Auth 활성화
vault auth enable kubernetes

# K8s 설정
vault write auth/kubernetes/config \
  kubernetes_host="https://$KUBERNETES_PORT_443_TCP_ADDR:443"

# Policy 생성
vault policy write external-secrets - <<EOF
path "secret/data/*" {
  capabilities = ["read", "list"]
}
EOF

# Role 생성
vault write auth/kubernetes/role/external-secrets \
  bound_service_account_names=vault-auth \
  bound_service_account_namespaces=staging,production \
  policies=external-secrets \
  ttl=1h
```

---

## 🛠️ Part 2: ESO 설치

```bash
helm repo add external-secrets https://charts.external-secrets.io
helm install external-secrets external-secrets/external-secrets \
  -n external-secrets \
  --create-namespace \
  --set installCRDs=true
```

---

## 🛠️ Part 3: SecretStore 설정

### Service Account

```yaml
# vault/service-account.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-auth
  namespace: staging
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-auth
  namespace: production
```

### ClusterSecretStore

```yaml
# vault/cluster-secret-store.yaml
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://vault.vault.svc:8200"
      path: "secret"
      version: "v2"
      auth:
        tokenSecretRef:
          name: vault-token
          namespace: external-secrets
          key: token
```

또는 K8s Auth:

```yaml
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: staging
spec:
  provider:
    vault:
      server: "http://vault.vault.svc:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "external-secrets"
          serviceAccountRef:
            name: vault-auth
```

---

## 🛠️ Part 4: ExternalSecret

### Staging

```yaml
# base/external-secret.yaml
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: myapp-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: myapp-secrets
  data:
  - secretKey: DB_HOST
    remoteRef:
      key: myapp/staging
      property: db_host
  - secretKey: DB_PASSWORD
    remoteRef:
      key: myapp/staging
      property: db_password
  - secretKey: API_KEY
    remoteRef:
      key: myapp/staging
      property: api_key
```

### Production (Overlay)

```yaml
# overlays/prod/external-secret-patch.yaml
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: myapp-secrets
spec:
  data:
  - secretKey: DB_HOST
    remoteRef:
      key: myapp/prod       # prod 경로로 변경
      property: db_host
  - secretKey: DB_PASSWORD
    remoteRef:
      key: myapp/prod
      property: db_password
  - secretKey: API_KEY
    remoteRef:
      key: myapp/prod
      property: api_key
```

---

## 🛠️ Part 5: Deployment에서 사용

```yaml
# base/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  template:
    spec:
      containers:
      - name: myapp
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: myapp-secrets
              key: DB_HOST
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: myapp-secrets
              key: DB_PASSWORD
```

---

## 🛠️ Part 6: 검증

```bash
# ExternalSecret 상태
kubectl get externalsecret -n staging

# 생성된 Secret 확인
kubectl get secret myapp-secrets -n staging -o yaml

# 값 확인
kubectl get secret myapp-secrets -n staging -o jsonpath='{.data.DB_PASSWORD}' | base64 -d
```

---

## ✅ 오늘의 체크리스트

| # | 항목 | 완료 |
|---|------|------|
| 1 | Vault 설치 | ☐ |
| 2 | Vault에 시크릿 저장 | ☐ |
| 3 | K8s Auth 설정 | ☐ |
| 4 | ESO 설치 | ☐ |
| 5 | SecretStore 생성 | ☐ |
| 6 | ExternalSecret 생성 | ☐ |
| 7 | K8s Secret 동기화 확인 | ☐ |

---

## ➡️ 다음 학습: Day 116

**주제**: Kustomize 환경 분리

