# 📅 Day 110: CI/CD 통합 실습

## 🎯 오늘의 목표

> ArgoCD + Vault + ESO를 통합하여 완전한 GitOps 파이프라인을 구축합니다.

Git 커밋부터 프로덕션 배포까지 전체 흐름을 자동화합니다.

---

## ⏰ 예상 학습 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| 아키텍처 설계 | 30분 | 전체 흐름 설계 |
| CI 구성 | 1시간 30분 | GitHub Actions |
| CD 구성 | 1시간 30분 | ArgoCD + Vault |
| 통합 테스트 | 30분 | End-to-End 검증 |

---

## 📚 Part 1: 전체 아키텍처

### CI/CD 통합 파이프라인

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                     Complete GitOps Pipeline Architecture                     │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   [Developer]                                                                │
│       │                                                                      │
│       │ git push                                                             │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        GitHub (App Repo)                             │   │
│   │   feat/payment-api → PR → Merge to main                             │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                      │
│       │ Trigger                                                              │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    CI: GitHub Actions                                │   │
│   │   1. Lint & Test                                                     │   │
│   │   2. Build Docker Image                                              │   │
│   │   3. Push to ECR (tag: commit-sha)                                  │   │
│   │   4. Update Infra Repo (kustomize set image)                        │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                      │
│       │ Update image tag                                                     │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                        GitHub (Infra Repo)                           │   │
│   │   overlays/staging/kustomization.yaml                               │   │
│   │   newTag: abc1234 (commit-sha)                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                      │
│       │ Detect change                                                        │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    CD: ArgoCD                                        │   │
│   │   Auto Sync → Apply manifests to K8s                                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                      │
│       │ Apply                                                                │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                    Kubernetes                                        │   │
│   │   ExternalSecret → SecretStore → Vault                              │   │
│   │   Deployment uses secrets from ESO                                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ Part 2: CI - GitHub Actions

### 워크플로우 작성

```yaml
# .github/workflows/ci.yaml
name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: tossplace/myapp
  INFRA_REPO: tossplace/infra-repo

jobs:
  # ========================================
  # Job 1: Lint and Test
  # ========================================
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'
    
    - name: Lint
      uses: golangci/golangci-lint-action@v3
    
    - name: Unit Tests
      run: go test -v -coverprofile=coverage.out ./...
    
    - name: Upload Coverage
      uses: codecov/codecov-action@v3

  # ========================================
  # Job 2: Security Scan
  # ========================================
  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        severity: 'CRITICAL,HIGH'
        exit-code: '1'

  # ========================================
  # Job 3: Build and Push
  # ========================================
  build:
    needs: [test, security]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}
    
    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2
    
    - name: Build, tag, and push image
      id: build
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: ${{ github.sha }}
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
    
    - name: Image meta
      id: meta
      run: echo "tags=${{ steps.build.outputs.image }}" >> $GITHUB_OUTPUT

  # ========================================
  # Job 4: Update Infra Repo
  # ========================================
  update-infra:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout infra repo
      uses: actions/checkout@v4
      with:
        repository: ${{ env.INFRA_REPO }}
        token: ${{ secrets.INFRA_REPO_TOKEN }}
        path: infra
    
    - name: Setup Kustomize
      uses: imranismail/setup-kustomize@v2
    
    - name: Update image tag (Staging)
      run: |
        cd infra/overlays/staging
        kustomize edit set image myapp=${{ needs.build.outputs.image_tag }}
    
    - name: Commit and Push
      run: |
        cd infra
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git add .
        git commit -m "chore: update myapp to ${{ github.sha }}"
        git push
```

---

## 🛠️ Part 3: CD - ArgoCD Application

### ArgoCD Application 정의

```yaml
# argocd/applications/myapp-staging.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: tossplace
  
  source:
    repoURL: https://github.com/tossplace/infra-repo.git
    path: overlays/staging
    targetRevision: main
  
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  
  syncPolicy:
    automated:
      prune: true      # 삭제된 리소스 정리
      selfHeal: true   # 수동 변경 자동 복구
    syncOptions:
    - CreateNamespace=true
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
```

### Production (수동 승인)

```yaml
# argocd/applications/myapp-production.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-production
  namespace: argocd
spec:
  project: tossplace
  
  source:
    repoURL: https://github.com/tossplace/infra-repo.git
    path: overlays/production
    targetRevision: main
  
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  
  syncPolicy:
    # automated 없음 = 수동 Sync
    syncOptions:
    - CreateNamespace=true
```

---

## 🛠️ Part 4: K8s 매니페스트 (Vault 연동)

### Kustomize Base

```yaml
# base/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      serviceAccountName: myapp
      containers:
      - name: myapp
        image: myapp:latest  # Kustomize가 교체
        ports:
        - containerPort: 8080
        env:
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: myapp-db-secret
              key: host
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: myapp-db-secret
              key: password
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
```

### ExternalSecret

```yaml
# base/external-secret.yaml
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: myapp-db-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: myapp-db-secret
  data:
  - secretKey: host
    remoteRef:
      key: myapp/database
      property: host
  - secretKey: port
    remoteRef:
      key: myapp/database
      property: port
  - secretKey: username
    remoteRef:
      key: myapp/database
      property: username
  - secretKey: password
    remoteRef:
      key: myapp/database
      property: password
```

### Kustomization

```yaml
# base/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- external-secret.yaml
- serviceaccount.yaml
```

```yaml
# overlays/staging/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
- ../../base

images:
- name: myapp
  newName: 123456789012.dkr.ecr.ap-northeast-2.amazonaws.com/myapp
  newTag: abc1234  # CI가 업데이트

replicas:
- name: myapp
  count: 2
```

---

## 🛠️ Part 5: End-to-End 테스트

### 전체 흐름 검증

```bash
# 1. Git Push (시뮬레이션)
echo "// feature change" >> main.go
git add . && git commit -m "feat: add new feature"
git push

# 2. GitHub Actions 확인
# GitHub UI에서 Actions 탭 확인

# 3. ArgoCD 확인
argocd app get myapp-staging
argocd app sync myapp-staging

# 4. K8s 확인
kubectl get pods -n staging -w
kubectl get externalsecret -n staging
kubectl get secret myapp-db-secret -n staging

# 5. 애플리케이션 테스트
kubectl port-forward svc/myapp -n staging 8080:8080
curl http://localhost:8080/health
```

### 롤백 테스트

```bash
# ArgoCD 히스토리 확인
argocd app history myapp-staging

# 롤백
argocd app rollback myapp-staging 1

# 또는 Git revert
cd infra-repo
git revert HEAD
git push  # ArgoCD 자동 Sync
```

---

## 📋 통합 체크리스트

| 단계 | 항목 | 확인 |
|------|------|------|
| CI | GitHub Actions 트리거 | ☐ |
| CI | 테스트 통과 | ☐ |
| CI | 이미지 빌드 | ☐ |
| CI | ECR 푸시 | ☐ |
| CI | Infra Repo 업데이트 | ☐ |
| CD | ArgoCD 감지 | ☐ |
| CD | 자동 Sync | ☐ |
| CD | ExternalSecret 동기화 | ☐ |
| CD | Pod Running | ☐ |
| CD | Health Check 통과 | ☐ |

---

## ✅ 오늘의 체크리스트

| # | 항목 | 완료 |
|---|------|------|
| 1 | 전체 파이프라인 아키텍처 이해 | ☐ |
| 2 | GitHub Actions CI 구성 | ☐ |
| 3 | ArgoCD Application 생성 | ☐ |
| 4 | ExternalSecret 설정 | ☐ |
| 5 | End-to-End 테스트 | ☐ |
| 6 | 롤백 테스트 | ☐ |

---

## ➡️ 다음 학습: Day 111

**주제**: Week 15 복습

