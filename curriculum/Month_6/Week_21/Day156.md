# 📅 Day 156: CloudWatch & CloudTrail - AWS 모니터링과 감사 로깅

## 🎯 오늘의 목표

> **토스플레이스 연결점**: "서비스 안정성을 위한 모니터링/로깅 시스템 구축 및 운영"

CloudWatch로 AWS 리소스를 모니터링하고, CloudTrail로 API 호출을 감사합니다.

---

## ⏰ 예상 학습 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| CloudWatch | 2시간 | 메트릭, 로그, 알람 |
| CloudTrail | 1시간 | 감사 로깅 |
| 실습 | 1시간 | CLI 및 통합 실습 |

---

## 📚 Part 1: Amazon CloudWatch (2시간)

### CloudWatch 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│  Amazon CloudWatch Architecture                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   Data Sources                       │   │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │   │
│  │  │ EC2  │ │ RDS  │ │ EKS  │ │Lambda│ │Custom│      │   │
│  │  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘      │   │
│  │     │        │        │        │        │           │   │
│  └─────┼────────┼────────┼────────┼────────┼───────────┘   │
│        │        │        │        │        │               │
│        ▼        ▼        ▼        ▼        ▼               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 CloudWatch                           │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │              Metrics                           │  │   │
│  │  │  - Standard Metrics (AWS 서비스)              │  │   │
│  │  │  - Custom Metrics (사용자 정의)               │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                       │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │                Logs                            │  │   │
│  │  │  - Log Groups / Log Streams                   │  │   │
│  │  │  - Metric Filters                             │  │   │
│  │  │  - Insights (쿼리)                            │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                       │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │               Alarms                           │  │   │
│  │  │  - 임계값 기반 알람                           │  │   │
│  │  │  - 복합 알람                                  │  │   │
│  │  │  - SNS, Lambda 연동                           │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                       │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │             Dashboards                         │  │   │
│  │  │  - 커스텀 대시보드                            │  │   │
│  │  │  - 실시간 모니터링                            │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### CloudWatch Metrics

```bash
# 사용 가능한 메트릭 네임스페이스 조회
aws cloudwatch list-metrics \
  --query 'Metrics[].Namespace' | sort | uniq

# EC2 인스턴스 CPU 메트릭 조회
aws cloudwatch get-metric-statistics \
  --namespace AWS/EC2 \
  --metric-name CPUUtilization \
  --dimensions Name=InstanceId,Value=i-1234567890abcdef0 \
  --start-time $(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%SZ) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%SZ) \
  --period 300 \
  --statistics Average Maximum

# EKS 클러스터 메트릭
aws cloudwatch get-metric-statistics \
  --namespace ContainerInsights \
  --metric-name pod_cpu_utilization \
  --dimensions Name=ClusterName,Value=my-cluster \
  --start-time $(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%SZ) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%SZ) \
  --period 60 \
  --statistics Average

# 커스텀 메트릭 전송
aws cloudwatch put-metric-data \
  --namespace "TossPlace/Application" \
  --metric-name "PaymentSuccess" \
  --value 1 \
  --unit Count \
  --dimensions Service=payment-api,Environment=prod
```

### CloudWatch Logs

```bash
# 로그 그룹 목록
aws logs describe-log-groups \
  --query 'logGroups[].[logGroupName,storedBytes]' \
  --output table

# 로그 스트림 조회
aws logs describe-log-streams \
  --log-group-name /aws/eks/my-cluster/cluster \
  --order-by LastEventTime \
  --descending \
  --limit 5

# 로그 이벤트 조회
aws logs get-log-events \
  --log-group-name /aws/eks/my-cluster/cluster \
  --log-stream-name kube-apiserver-xxx \
  --limit 100

# CloudWatch Logs Insights 쿼리
aws logs start-query \
  --log-group-name /aws/eks/my-cluster/cluster \
  --start-time $(date -d '1 hour ago' +%s) \
  --end-time $(date +%s) \
  --query-string '
    fields @timestamp, @message
    | filter @message like /ERROR/
    | sort @timestamp desc
    | limit 50
  '

# 쿼리 결과 조회
aws logs get-query-results \
  --query-id "xxxx-xxxx-xxxx"
```

### CloudWatch Alarms

```bash
# 알람 생성 - CPU 사용률 80% 초과
aws cloudwatch put-metric-alarm \
  --alarm-name "High-CPU-Alarm" \
  --alarm-description "CPU utilization exceeds 80%" \
  --metric-name CPUUtilization \
  --namespace AWS/EC2 \
  --statistic Average \
  --period 300 \
  --threshold 80 \
  --comparison-operator GreaterThanThreshold \
  --dimensions Name=InstanceId,Value=i-1234567890abcdef0 \
  --evaluation-periods 2 \
  --alarm-actions arn:aws:sns:ap-northeast-2:123456789012:alerts \
  --treat-missing-data notBreaching

# RDS 연결 수 알람
aws cloudwatch put-metric-alarm \
  --alarm-name "RDS-High-Connections" \
  --metric-name DatabaseConnections \
  --namespace AWS/RDS \
  --statistic Average \
  --period 60 \
  --threshold 100 \
  --comparison-operator GreaterThanThreshold \
  --dimensions Name=DBInstanceIdentifier,Value=my-db \
  --evaluation-periods 3 \
  --alarm-actions arn:aws:sns:ap-northeast-2:123456789012:alerts

# 알람 목록 조회
aws cloudwatch describe-alarms \
  --query 'MetricAlarms[].[AlarmName,StateValue,MetricName]' \
  --output table

# 알람 상태 변경 히스토리
aws cloudwatch describe-alarm-history \
  --alarm-name "High-CPU-Alarm" \
  --history-item-type StateUpdate
```

### CloudWatch Dashboard

```json
// dashboard.json
{
  "widgets": [
    {
      "type": "metric",
      "x": 0,
      "y": 0,
      "width": 12,
      "height": 6,
      "properties": {
        "title": "EC2 CPU Utilization",
        "metrics": [
          ["AWS/EC2", "CPUUtilization", "InstanceId", "i-xxx"]
        ],
        "period": 300,
        "stat": "Average",
        "region": "ap-northeast-2"
      }
    },
    {
      "type": "metric",
      "x": 12,
      "y": 0,
      "width": 12,
      "height": 6,
      "properties": {
        "title": "RDS Connections",
        "metrics": [
          ["AWS/RDS", "DatabaseConnections", "DBInstanceIdentifier", "my-db"]
        ],
        "period": 60,
        "stat": "Average"
      }
    },
    {
      "type": "log",
      "x": 0,
      "y": 6,
      "width": 24,
      "height": 6,
      "properties": {
        "title": "Error Logs",
        "query": "fields @timestamp, @message | filter @message like /ERROR/ | sort @timestamp desc | limit 20",
        "region": "ap-northeast-2",
        "view": "table"
      }
    }
  ]
}
```

```bash
# 대시보드 생성
aws cloudwatch put-dashboard \
  --dashboard-name "TossPlace-Operations" \
  --dashboard-body file://dashboard.json
```

---

## 📚 Part 2: AWS CloudTrail (1시간)

### CloudTrail 개요

```
┌─────────────────────────────────────────────────────────────┐
│  AWS CloudTrail Architecture                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 API Calls                            │   │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐              │   │
│  │  │ AWS  │ │ CLI  │ │ SDK  │ │Console│              │   │
│  │  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘              │   │
│  │     │        │        │        │                    │   │
│  └─────┼────────┼────────┼────────┼────────────────────┘   │
│        │        │        │        │                        │
│        ▼        ▼        ▼        ▼                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  CloudTrail                          │   │
│  │                                                       │   │
│  │  기록 내용:                                          │   │
│  │  - 누가 (IAM User/Role)                             │   │
│  │  - 언제 (Timestamp)                                  │   │
│  │  - 무엇을 (API Action)                              │   │
│  │  - 어디서 (Source IP)                               │   │
│  │  - 결과 (Success/Failure)                           │   │
│  │                                                       │   │
│  └───────────────────────┬─────────────────────────────┘   │
│                          │                                  │
│           ┌──────────────┼──────────────┐                  │
│           ▼              ▼              ▼                  │
│      ┌────────┐    ┌────────┐    ┌────────┐               │
│      │   S3   │    │  CW    │    │ Event  │               │
│      │Bucket  │    │ Logs   │    │ Bridge │               │
│      └────────┘    └────────┘    └────────┘               │
│                                                             │
│  토스플레이스 활용:                                         │
│  - 보안 감사 (PCI-DSS 컴플라이언스)                        │
│  - 이상 행위 탐지                                          │
│  - 변경 추적                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### CloudTrail 설정 및 조회

```bash
# Trail 목록
aws cloudtrail describe-trails \
  --query 'trailList[].[Name,S3BucketName,IsMultiRegionTrail]' \
  --output table

# Trail 상태 확인
aws cloudtrail get-trail-status \
  --name my-trail

# 최근 이벤트 조회
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventSource,AttributeValue=ec2.amazonaws.com \
  --start-time $(date -d '1 hour ago' +%Y-%m-%dT%H:%M:%SZ) \
  --query 'Events[].[EventTime,EventName,Username]' \
  --output table

# 특정 사용자 활동 조회
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=Username,AttributeValue=admin@toss.im \
  --start-time $(date -d '24 hours ago' +%Y-%m-%dT%H:%M:%SZ)

# 실패한 API 호출 조회
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=ReadOnly,AttributeValue=false \
  --query 'Events[?contains(CloudTrailEvent, `"errorCode"`)].CloudTrailEvent'
```

### 보안 이벤트 모니터링

```bash
# IAM 정책 변경 모니터링
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=PutUserPolicy

# 보안 그룹 변경
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=AuthorizeSecurityGroupIngress

# 콘솔 로그인 이벤트
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=ConsoleLogin
```

### CloudTrail + CloudWatch Logs 연동

```bash
# Metric Filter 생성 - 루트 계정 사용 감지
aws logs put-metric-filter \
  --log-group-name CloudTrail/logs \
  --filter-name "RootAccountUsage" \
  --filter-pattern '{ $.userIdentity.type = "Root" && $.userIdentity.invokedBy NOT EXISTS && $.eventType != "AwsServiceEvent" }' \
  --metric-transformations \
    metricName=RootAccountUsageCount,metricNamespace=CloudTrailMetrics,metricValue=1

# 알람 생성
aws cloudwatch put-metric-alarm \
  --alarm-name "Root-Account-Usage" \
  --metric-name RootAccountUsageCount \
  --namespace CloudTrailMetrics \
  --statistic Sum \
  --period 300 \
  --threshold 1 \
  --comparison-operator GreaterThanOrEqualToThreshold \
  --evaluation-periods 1 \
  --alarm-actions arn:aws:sns:ap-northeast-2:123456789012:security-alerts
```

---

## 🛠️ Part 3: 통합 실습 (1시간)

### EKS 모니터링 설정

```bash
# Container Insights 활성화
aws eks update-cluster-config \
  --name my-cluster \
  --logging '{"clusterLogging":[{"types":["api","audit","authenticator","controllerManager","scheduler"],"enabled":true}]}'

# CloudWatch Agent 설치 (DaemonSet)
kubectl apply -f https://raw.githubusercontent.com/aws-samples/amazon-cloudwatch-container-insights/latest/k8s-deployment-manifest-templates/deployment-mode/daemonset/container-insights-monitoring/quickstart/cwagent-serviceaccount.yaml
```

### 종합 모니터링 대시보드

```bash
# 주요 메트릭 한 번에 조회
cat << 'EOF' > get-metrics.sh
#!/bin/bash

echo "=== EC2 CPU ==="
aws cloudwatch get-metric-statistics \
  --namespace AWS/EC2 \
  --metric-name CPUUtilization \
  --dimensions Name=InstanceId,Value=$1 \
  --start-time $(date -u -d '30 minutes ago' +%Y-%m-%dT%H:%M:%SZ) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%SZ) \
  --period 60 \
  --statistics Average \
  --query 'Datapoints | sort_by(@, &Timestamp) | [-1].Average'

echo "=== RDS Connections ==="
aws cloudwatch get-metric-statistics \
  --namespace AWS/RDS \
  --metric-name DatabaseConnections \
  --dimensions Name=DBInstanceIdentifier,Value=$2 \
  --start-time $(date -u -d '30 minutes ago' +%Y-%m-%dT%H:%M:%SZ) \
  --end-time $(date -u +%Y-%m-%dT%H:%M:%SZ) \
  --period 60 \
  --statistics Average \
  --query 'Datapoints | sort_by(@, &Timestamp) | [-1].Average'

echo "=== Active Alarms ==="
aws cloudwatch describe-alarms \
  --state-value ALARM \
  --query 'MetricAlarms[].[AlarmName,MetricName]' \
  --output table
EOF

chmod +x get-metrics.sh
./get-metrics.sh i-xxx my-db
```

---

## ✅ 오늘의 체크리스트

| # | 항목 | 완료 |
|---|------|------|
| 1 | CloudWatch 메트릭 조회 | ☐ |
| 2 | CloudWatch 알람 생성 | ☐ |
| 3 | CloudWatch Logs 쿼리 | ☐ |
| 4 | CloudWatch Dashboard 생성 | ☐ |
| 5 | CloudTrail 이벤트 조회 | ☐ |
| 6 | 보안 이벤트 모니터링 설정 | ☐ |
| 7 | CloudTrail + CloudWatch 연동 | ☐ |

---

## 🔑 핵심 명령어 정리

```bash
# CloudWatch
aws cloudwatch get-metric-statistics  # 메트릭 조회
aws cloudwatch put-metric-alarm       # 알람 생성
aws cloudwatch describe-alarms        # 알람 목록
aws logs get-log-events               # 로그 조회
aws logs start-query                  # Logs Insights 쿼리

# CloudTrail
aws cloudtrail lookup-events          # 이벤트 조회
aws cloudtrail describe-trails        # Trail 목록
aws cloudtrail get-trail-status       # Trail 상태
```

---

## 📝 면접 대비 질문

### Q1: CloudWatch와 Prometheus의 차이점은?
> "CloudWatch는 AWS 네이티브 서비스로 AWS 리소스와 긴밀하게 통합되어 있습니다. Prometheus는 오픈소스로 Kubernetes 환경에 특화되어 있고 PromQL로 유연한 쿼리가 가능합니다. 토스플레이스에서는 AWS 리소스는 CloudWatch, EKS 워크로드는 Prometheus + Thanos로 모니터링하여 각각의 장점을 활용합니다."

### Q2: CloudTrail은 왜 중요한가요?
> "CloudTrail은 모든 AWS API 호출을 기록하여 '누가, 언제, 무엇을, 어디서' 했는지 추적합니다. PCI-DSS 같은 보안 컴플라이언스를 위해 필수이며, 보안 사고 조사, 이상 행위 탐지, 변경 관리에 사용됩니다. 토스플레이스 같은 결제 시스템에서는 감사 로그가 규제 요건입니다."

---

## ➡️ 다음 학습: Day 157

**주제**: Week 21 종합 복습
- VPC, IAM, EKS, S3, RDS 정리
- 핵심 CLI 명령어 복습
- 실전 시나리오 문제 풀이
