# 📅 Day 172: 최종 프로젝트 시작 - 토스플레이스 인프라 클론

## 🎯 오늘의 목표

> **토스플레이스 최종 포트폴리오**: 180일 학습의 집대성 - 실제 프로덕션 수준 DevOps 인프라 구축

오늘부터 9일간 **토스플레이스 인프라를 클론**하는 최종 프로젝트를 진행합니다. Terraform, EKS, Istio, ArgoCD, Prometheus, Thanos, Grafana를 모두 통합한 완전한 DevOps 플랫폼을 구축합니다.

---

## ⏰ 예상 학습 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| 설계 | 2시간 | 아키텍처 설계 |
| 계획 | 1시간 | 마일스톤 설정 |
| 준비 | 1시간 | 환경 설정 |

---

## 📚 Part 1: 프로젝트 개요 (30분)

### 프로젝트 목표

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🎯 최종 프로젝트: 토스플레이스 DevOps 인프라 클론                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✅ 구현 목표:                                                              │
│  1. AWS 기반 프로덕션 수준 인프라 (VPC, EKS, RDS)                           │
│  2. Service Mesh (Istio) - 트래픽 관리, mTLS                               │
│  3. GitOps (ArgoCD) - 자동 배포 파이프라인                                  │
│  4. 모니터링 스택 (Prometheus + Thanos + Grafana)                          │
│  5. 전체 인프라 IaC (Terraform)                                            │
│                                                                             │
│  ✅ 포트폴리오 가치:                                                        │
│  - 토스플레이스 기술 스택 완벽 재현                                        │
│  - 면접에서 실제 구현 경험 설명 가능                                        │
│  - GitHub에 공개하여 기술력 증명                                           │
│                                                                             │
│  ✅ 채용공고 요건 매칭:                                                     │
│  - Kubernetes 클러스터 운영/최적화 ✓                                       │
│  - Service Mesh (Istio) 경험 ✓                                             │
│  - CI/CD (ArgoCD) ✓                                                        │
│  - 모니터링 (Prometheus, Thanos, Grafana) ✓                                │
│  - IaC 경험 ✓                                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 최종 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     토스플레이스 인프라 클론 아키텍처                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                        AWS Cloud (ap-northeast-2)                   │   │
│  │                                                                     │   │
│  │  ┌───────────────────── VPC (10.0.0.0/16) ────────────────────┐   │   │
│  │  │                                                              │   │   │
│  │  │  ┌──────────────────────────────────────────────────────┐  │   │   │
│  │  │  │               EKS Cluster (1.28)                      │  │   │   │
│  │  │  │                                                       │  │   │   │
│  │  │  │  ┌─────────────────────────────────────────────────┐ │  │   │   │
│  │  │  │  │            Istio Service Mesh                    │ │  │   │   │
│  │  │  │  │                                                  │ │  │   │   │
│  │  │  │  │  ┌──────────────────────────────────────────┐  │ │  │   │   │
│  │  │  │  │  │         Application Namespace             │  │ │  │   │   │
│  │  │  │  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐   │  │ │  │   │   │
│  │  │  │  │  │  │Frontend │→│ Backend │→│  Cache  │   │  │ │  │   │   │
│  │  │  │  │  │  │ (React) │ │ (Spring)│ │ (Redis) │   │  │ │  │   │   │
│  │  │  │  │  │  └─────────┘ └─────────┘ └─────────┘   │  │ │  │   │   │
│  │  │  │  │  │                    │                    │  │ │  │   │   │
│  │  │  │  │  │                    ▼                    │  │ │  │   │   │
│  │  │  │  │  │            ┌───────────┐               │  │ │  │   │   │
│  │  │  │  │  │            │ RDS MySQL │               │  │ │  │   │   │
│  │  │  │  │  │            │ (Multi-AZ)│               │  │ │  │   │   │
│  │  │  │  │  │            └───────────┘               │  │ │  │   │   │
│  │  │  │  │  └──────────────────────────────────────────┘  │ │  │   │   │
│  │  │  │  │                                                  │ │  │   │   │
│  │  │  │  │  ┌──────────────────────────────────────────┐  │ │  │   │   │
│  │  │  │  │  │         Platform Namespace                │  │ │  │   │   │
│  │  │  │  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐   │  │ │  │   │   │
│  │  │  │  │  │  │ ArgoCD  │ │Prometheus│ │ Grafana │   │  │ │  │   │   │
│  │  │  │  │  │  └─────────┘ └─────────┘ └─────────┘   │  │ │  │   │   │
│  │  │  │  │  │                    │                    │  │ │  │   │   │
│  │  │  │  │  │                    ▼                    │  │ │  │   │   │
│  │  │  │  │  │  ┌─────────────────────────────────┐   │  │ │  │   │   │
│  │  │  │  │  │  │   Thanos (S3 Long-term Storage) │   │  │ │  │   │   │
│  │  │  │  │  │  └─────────────────────────────────┘   │  │ │  │   │   │
│  │  │  │  │  └──────────────────────────────────────────┘  │ │  │   │   │
│  │  │  │  │                                                  │ │  │   │   │
│  │  │  │  └─────────────────────────────────────────────────┘ │  │   │   │
│  │  │  │                                                       │  │   │   │
│  │  │  └──────────────────────────────────────────────────────┘  │   │   │
│  │  │                                                              │   │   │
│  │  └──────────────────────────────────────────────────────────────┘   │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        CI/CD Pipeline                                │   │
│  │  GitHub → GitHub Actions → ECR → ArgoCD → EKS                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  🔧 모든 인프라는 Terraform으로 코드화                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📅 Part 2: 프로젝트 마일스톤 (30분)

### 9일 일정

| Day | 날짜 | 작업 | 산출물 |
|-----|------|------|--------|
| **172** | 오늘 | 설계 & 계획 | 아키텍처 문서 |
| **173** | +1일 | Terraform 인프라 | VPC, EKS, RDS |
| **174** | +2일 | Istio 설정 | Service Mesh |
| **175** | +3일 | ArgoCD 설정 | GitOps 파이프라인 |
| **176** | +4일 | 모니터링 스택 | Prometheus, Grafana, Thanos |
| **177** | +5일 | 문서화 | README, 아키텍처 다이어그램 |
| **178** | +6일 | 테스트 | E2E 검증, 장애 시뮬레이션 |
| **179** | +7일 | 면접 준비 | Q&A 연습 |
| **180** | +8일 | 완료 | GitHub 공개 |

### 상세 작업 분해 (WBS)

```
┌─────────────────────────────────────────────────────────────┐
│  프로젝트 WBS                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 인프라 계층 (Day 172-173)                               │
│  ├── 1.1 VPC 모듈 구현                                      │
│  │   ├── Public/Private Subnet                              │
│  │   ├── NAT Gateway                                        │
│  │   └── VPC Flow Logs                                      │
│  ├── 1.2 EKS 모듈 구현                                      │
│  │   ├── Cluster                                            │
│  │   ├── Node Groups (General, Spot)                        │
│  │   ├── OIDC Provider                                      │
│  │   └── Addons (VPC CNI, CoreDNS, EBS CSI)                │
│  └── 1.3 RDS 모듈 구현                                      │
│      ├── MySQL Multi-AZ                                     │
│      ├── Secrets Manager 연동                               │
│      └── Parameter Group                                    │
│                                                             │
│  2. 플랫폼 계층 (Day 174-176)                               │
│  ├── 2.1 Istio                                              │
│  │   ├── 설치 (Production Profile)                         │
│  │   ├── Gateway                                            │
│  │   ├── VirtualService/DestinationRule                    │
│  │   └── mTLS (STRICT mode)                                │
│  ├── 2.2 ArgoCD                                             │
│  │   ├── 설치                                               │
│  │   ├── Application 설정                                   │
│  │   ├── Auto Sync                                          │
│  │   └── Notification (Slack)                               │
│  └── 2.3 모니터링                                           │
│      ├── kube-prometheus-stack                              │
│      ├── Thanos Sidecar + Query + Store                    │
│      ├── Grafana 대시보드                                   │
│      └── AlertManager + Slack                               │
│                                                             │
│  3. 애플리케이션 계층 (Day 175-176)                         │
│  ├── 3.1 샘플 앱 배포                                       │
│  │   ├── Frontend (React/Nginx)                             │
│  │   ├── Backend (Spring Boot)                              │
│  │   └── Helm Chart / Kustomize                             │
│  └── 3.2 GitOps 설정                                        │
│      ├── App Repo                                           │
│      ├── Infra Repo                                         │
│      └── ArgoCD Application                                 │
│                                                             │
│  4. 문서화 & 완료 (Day 177-180)                             │
│  ├── 4.1 README.md                                          │
│  ├── 4.2 아키텍처 다이어그램                                │
│  ├── 4.3 배포 가이드                                        │
│  ├── 4.4 트러블슈팅 가이드                                  │
│  └── 4.5 면접 Q&A 준비                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ Part 3: 프로젝트 설정 (2시간)

### 실습 1: GitHub 레포지토리 구조 (30분)

```bash
# 1. 레포지토리 생성
# GitHub에서 'tossplace-infra-clone' 레포지토리 생성

# 2. 로컬 클론
git clone https://github.com/YOUR_USERNAME/tossplace-infra-clone
cd tossplace-infra-clone

# 3. 디렉토리 구조 생성
mkdir -p {infrastructure/{modules/{vpc,eks,rds,elasticache},environments/{dev,prod}},kubernetes/{base,overlays/{dev,prod},argocd},docs,scripts}

# 4. 구조 확인
tree -L 3
```

**예상 디렉토리 구조**:

```
tossplace-infra-clone/
├── infrastructure/                 # Terraform 코드
│   ├── modules/                   # 재사용 가능한 모듈
│   │   ├── vpc/
│   │   │   ├── main.tf
│   │   │   ├── variables.tf
│   │   │   └── outputs.tf
│   │   ├── eks/
│   │   ├── rds/
│   │   └── elasticache/
│   ├── environments/              # 환경별 구성
│   │   ├── dev/
│   │   │   ├── main.tf
│   │   │   ├── variables.tf
│   │   │   ├── outputs.tf
│   │   │   ├── terraform.tfvars
│   │   │   └── backend.tf
│   │   └── prod/
│   └── .github/
│       └── workflows/
│           └── terraform.yml
├── kubernetes/                    # K8s 매니페스트
│   ├── base/                     # Kustomize base
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── kustomization.yaml
│   ├── overlays/                 # 환경별 overlay
│   │   ├── dev/
│   │   └── prod/
│   └── argocd/                   # ArgoCD 설정
│       ├── application.yaml
│       └── project.yaml
├── monitoring/                    # 모니터링 설정
│   ├── prometheus/
│   ├── grafana/
│   └── thanos/
├── istio/                        # Istio 설정
│   ├── gateway.yaml
│   ├── virtualservice.yaml
│   └── destination-rule.yaml
├── docs/                         # 문서
│   ├── architecture.md
│   └── deployment-guide.md
├── scripts/                      # 유틸리티 스크립트
│   ├── setup.sh
│   └── cleanup.sh
├── .gitignore
└── README.md
```

### 실습 2: 기본 파일 생성 (30분)

```bash
# .gitignore
cat > .gitignore << 'EOF'
# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!example.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets
*.pem
*.key
secrets/

# Logs
*.log
EOF
```

```bash
# README.md 초안
cat > README.md << 'EOF'
# 🚀 토스플레이스 DevOps 인프라 클론

> AWS 기반 프로덕션 수준 DevOps 플랫폼 구축 프로젝트

## 📋 프로젝트 개요

토스플레이스의 DevOps 인프라를 클론하여 실제 프로덕션 환경과 동일한 수준의 
Cloud Native 플랫폼을 구축합니다.

## 🏗️ 아키텍처

[아키텍처 다이어그램 - Day 177에서 추가 예정]

## 🛠️ 기술 스택

| 영역 | 기술 | 버전 |
|------|------|------|
| **IaC** | Terraform | 1.6+ |
| **Container Orchestration** | Amazon EKS | 1.28 |
| **Service Mesh** | Istio | 1.20 |
| **GitOps** | ArgoCD | 2.9 |
| **Monitoring** | Prometheus | 2.48 |
| **Visualization** | Grafana | 10.2 |
| **Long-term Storage** | Thanos | 0.32 |
| **Database** | Amazon RDS (MySQL) | 8.0 |
| **Cache** | ElastiCache (Redis) | 7.0 |

## 📁 디렉토리 구조

```
├── infrastructure/     # Terraform 인프라 코드
├── kubernetes/         # K8s 매니페스트
├── monitoring/         # 모니터링 설정
├── istio/             # Service Mesh 설정
├── docs/              # 문서
└── scripts/           # 유틸리티 스크립트
```

## 🚀 Quick Start

```bash
# 1. 레포지토리 클론
git clone https://github.com/YOUR_USERNAME/tossplace-infra-clone
cd tossplace-infra-clone

# 2. AWS 자격 증명 설정
aws configure

# 3. Terraform 인프라 배포
cd infrastructure/environments/dev
terraform init
terraform apply

# 4. kubectl 설정
aws eks update-kubeconfig --name tossplace-dev --region ap-northeast-2

# 5. 플랫폼 컴포넌트 설치
./scripts/setup.sh
```

## 📚 문서

- [아키텍처 상세](./docs/architecture.md)
- [배포 가이드](./docs/deployment-guide.md)
- [트러블슈팅](./docs/troubleshooting.md)

## 🎯 토스플레이스 기술 요건 매칭

| 요건 | 구현 | 상태 |
|------|------|------|
| Kubernetes 클러스터 운영 | EKS + Terraform | ✅ |
| Service Mesh (Istio) | VirtualService, mTLS | ✅ |
| CI/CD (ArgoCD) | GitOps 파이프라인 | ✅ |
| 모니터링 (Prometheus, Thanos, Grafana) | Full Stack | ✅ |
| IaC | Terraform 모듈화 | ✅ |

## 📝 학습 포인트

1. **인프라 코드화**: 전체 AWS 인프라를 Terraform으로 관리
2. **GitOps**: ArgoCD를 통한 선언적 배포
3. **Service Mesh**: Istio를 통한 트래픽 관리 및 보안
4. **관찰성**: Prometheus + Thanos + Grafana로 완전한 모니터링

## 📞 Contact

- GitHub: [@YOUR_USERNAME](https://github.com/YOUR_USERNAME)
- Email: your.email@example.com

---

*이 프로젝트는 토스플레이스 DevOps Engineer 포지션 준비를 위해 제작되었습니다.*
EOF
```

### 실습 3: Terraform Backend 준비 (30분)

```bash
# State 저장용 S3 버킷 및 DynamoDB 테이블 생성
# (이 부분은 수동으로 먼저 생성하거나 별도 Terraform으로 관리)

cat > infrastructure/global/backend/main.tf << 'EOF'
# Terraform Backend Resources
# 이 파일은 다른 Terraform 코드보다 먼저 수동으로 apply 해야 합니다.

terraform {
  required_version = ">= 1.5.0"
  
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "ap-northeast-2"
}

# S3 Bucket for Terraform State
resource "aws_s3_bucket" "terraform_state" {
  bucket = "tossplace-clone-terraform-state-${data.aws_caller_identity.current.account_id}"
  
  lifecycle {
    prevent_destroy = true
  }
  
  tags = {
    Name        = "Terraform State"
    Project     = "tossplace-clone"
    ManagedBy   = "Terraform"
  }
}

resource "aws_s3_bucket_versioning" "terraform_state" {
  bucket = aws_s3_bucket.terraform_state.id
  versioning_configuration {
    status = "Enabled"
  }
}

resource "aws_s3_bucket_server_side_encryption_configuration" "terraform_state" {
  bucket = aws_s3_bucket.terraform_state.id

  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "aws:kms"
    }
    bucket_key_enabled = true
  }
}

resource "aws_s3_bucket_public_access_block" "terraform_state" {
  bucket = aws_s3_bucket.terraform_state.id

  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# DynamoDB Table for State Locking
resource "aws_dynamodb_table" "terraform_lock" {
  name         = "tossplace-clone-terraform-lock"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "LockID"

  attribute {
    name = "LockID"
    type = "S"
  }

  tags = {
    Name        = "Terraform Lock"
    Project     = "tossplace-clone"
    ManagedBy   = "Terraform"
  }
}

data "aws_caller_identity" "current" {}

output "state_bucket_name" {
  value = aws_s3_bucket.terraform_state.bucket
}

output "lock_table_name" {
  value = aws_dynamodb_table.terraform_lock.name
}
EOF

# Backend 리소스 생성 (최초 1회)
cd infrastructure/global/backend
terraform init
terraform apply -auto-approve
```

### 실습 4: 환경별 Backend 설정 (30분)

```bash
# Dev 환경 Backend 설정
cat > infrastructure/environments/dev/backend.tf << 'EOF'
terraform {
  backend "s3" {
    bucket         = "tossplace-clone-terraform-state-ACCOUNT_ID"  # 실제 버킷 이름으로 변경
    key            = "dev/terraform.tfstate"
    region         = "ap-northeast-2"
    encrypt        = true
    dynamodb_table = "tossplace-clone-terraform-lock"
  }
}
EOF

# Prod 환경 Backend 설정
cat > infrastructure/environments/prod/backend.tf << 'EOF'
terraform {
  backend "s3" {
    bucket         = "tossplace-clone-terraform-state-ACCOUNT_ID"  # 실제 버킷 이름으로 변경
    key            = "prod/terraform.tfstate"
    region         = "ap-northeast-2"
    encrypt        = true
    dynamodb_table = "tossplace-clone-terraform-lock"
  }
}
EOF
```

---

## ✅ 오늘의 체크리스트

완료하면 체크하세요:

| # | 항목 | 완료 |
|---|------|------|
| 1 | 프로젝트 목표 및 범위 이해 | ☐ |
| 2 | 전체 아키텍처 설계 완료 | ☐ |
| 3 | 9일 마일스톤 계획 수립 | ☐ |
| 4 | GitHub 레포지토리 생성 및 구조화 | ☐ |
| 5 | Terraform Backend 리소스 생성 | ☐ |
| 6 | README.md 초안 작성 | ☐ |

---

## 🔑 오늘 생성한 핵심 파일

```
tossplace-infra-clone/
├── README.md                              # 프로젝트 소개
├── .gitignore                             # Git 제외 파일
├── infrastructure/
│   └── global/
│       └── backend/
│           └── main.tf                    # State 저장소
└── docs/
    └── architecture.md                    # 아키텍처 문서 (예정)
```

---

## 📝 학습 기록

```
학습일: ____년 __월 __일
실제 소요 시간: ____시간

어려웠던 점:



새로 알게 된 것:



추가로 궁금한 점:



```

---

## ➡️ 다음 학습: Day 173

**주제**: Terraform 인프라 구축 - VPC, EKS, RDS

**예고**:
- VPC 모듈 구현 (3-Tier Subnet)
- EKS 모듈 구현 (Node Groups, OIDC)
- RDS 모듈 구현 (Secrets Manager 연동)
- 환경별 구성 적용
- terraform apply로 인프라 생성

**준비물**:
- AWS 계정 및 IAM 권한
- Terraform CLI 설치
- AWS CLI 설정

