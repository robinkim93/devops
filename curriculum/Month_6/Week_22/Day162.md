# 📅 Day 162: Terraform EKS 구축

## 🎯 오늘의 목표

> **토스플레이스 핵심 인프라**: Terraform으로 프로덕션 수준의 EKS 클러스터를 구축합니다.

---

## ⏰ 예상 학습 시간: 4시간

| 구분 | 시간 | 내용 |
|------|------|------|
| EKS 아키텍처 | 30분 | 구성 요소 이해 |
| EKS 모듈 | 2시간 | Terraform 코드 |
| 실습 | 1시간 | 배포 및 검증 |
| 트러블슈팅 | 30분 | 문제 해결 |

---

## 📚 Part 1: EKS 아키텍처

```
┌────────────────────────────────────────────────────────────────┐
│                    EKS Cluster Architecture                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │              Control Plane (AWS Managed)                │  │
│   │   ┌───────────┐  ┌───────────┐  ┌───────────┐          │  │
│   │   │ API Server│  │etcd       │  │Controllers│          │  │
│   │   └───────────┘  └───────────┘  └───────────┘          │  │
│   └─────────────────────────────────────────────────────────┘  │
│                              │                                 │
│                              ▼                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │              Data Plane (Customer Managed)              │  │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │  │
│   │   │ Node Group  │  │ Node Group  │  │ Node Group  │    │  │
│   │   │ (general)   │  │ (spot)      │  │ (gpu)       │    │  │
│   │   │ t3.large    │  │ t3.medium   │  │ p3.2xlarge  │    │  │
│   │   └─────────────┘  └─────────────┘  └─────────────┘    │  │
│   └─────────────────────────────────────────────────────────┘  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### EKS 핵심 구성 요소

| 구성 요소 | 설명 | 관리 |
|----------|------|------|
| Control Plane | API Server, etcd, Controllers | AWS 관리 |
| Managed Node Groups | EC2 노드 그룹 | AWS + 사용자 |
| OIDC Provider | IAM 역할 연동 | 사용자 설정 |
| Addons | vpc-cni, coredns 등 | AWS 관리 가능 |

---

## 🛠️ Part 2: EKS 모듈

### variables.tf

```hcl
# modules/eks/variables.tf
variable "name" {
  description = "Cluster name"
  type        = string
}

variable "cluster_version" {
  description = "Kubernetes version"
  type        = string
  default     = "1.28"
}

variable "vpc_id" {
  description = "VPC ID"
  type        = string
}

variable "subnet_ids" {
  description = "Subnet IDs for node groups"
  type        = list(string)
}

variable "node_groups" {
  description = "EKS node groups configuration"
  type = map(object({
    instance_types = list(string)
    min_size       = number
    max_size       = number
    desired_size   = number
    capacity_type  = string
  }))
  default = {
    general = {
      instance_types = ["t3.large"]
      min_size       = 2
      max_size       = 4
      desired_size   = 2
      capacity_type  = "ON_DEMAND"
    }
  }
}

variable "tags" {
  type    = map(string)
  default = {}
}
```

### main.tf - EKS Cluster

```hcl
# modules/eks/main.tf

# EKS Cluster IAM Role
resource "aws_iam_role" "cluster" {
  name = "${var.name}-cluster-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Action = "sts:AssumeRole"
      Effect = "Allow"
      Principal = {
        Service = "eks.amazonaws.com"
      }
    }]
  })
}

resource "aws_iam_role_policy_attachment" "cluster_policy" {
  policy_arn = "arn:aws:iam::aws:policy/AmazonEKSClusterPolicy"
  role       = aws_iam_role.cluster.name
}

# EKS Cluster
resource "aws_eks_cluster" "main" {
  name     = var.name
  version  = var.cluster_version
  role_arn = aws_iam_role.cluster.arn

  vpc_config {
    subnet_ids              = var.subnet_ids
    endpoint_private_access = true
    endpoint_public_access  = true
  }

  enabled_cluster_log_types = ["api", "audit", "authenticator"]

  depends_on = [aws_iam_role_policy_attachment.cluster_policy]

  tags = var.tags
}

# OIDC Provider for IRSA
data "tls_certificate" "eks" {
  url = aws_eks_cluster.main.identity[0].oidc[0].issuer
}

resource "aws_iam_openid_connect_provider" "eks" {
  client_id_list  = ["sts.amazonaws.com"]
  thumbprint_list = [data.tls_certificate.eks.certificates[0].sha1_fingerprint]
  url             = aws_eks_cluster.main.identity[0].oidc[0].issuer
}
```

### main.tf - Node Group

```hcl
# modules/eks/main.tf (계속)

# Node Group IAM Role
resource "aws_iam_role" "node_group" {
  name = "${var.name}-node-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Action = "sts:AssumeRole"
      Effect = "Allow"
      Principal = {
        Service = "ec2.amazonaws.com"
      }
    }]
  })
}

resource "aws_iam_role_policy_attachment" "node_policies" {
  for_each = toset([
    "arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy",
    "arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy",
    "arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly",
  ])

  policy_arn = each.value
  role       = aws_iam_role.node_group.name
}

# EKS Node Groups
resource "aws_eks_node_group" "main" {
  for_each = var.node_groups

  cluster_name    = aws_eks_cluster.main.name
  node_group_name = each.key
  node_role_arn   = aws_iam_role.node_group.arn
  subnet_ids      = var.subnet_ids
  instance_types  = each.value.instance_types
  capacity_type   = each.value.capacity_type

  scaling_config {
    min_size     = each.value.min_size
    max_size     = each.value.max_size
    desired_size = each.value.desired_size
  }

  update_config {
    max_unavailable = 1
  }

  labels = {
    node-group = each.key
  }

  depends_on = [aws_iam_role_policy_attachment.node_policies]

  tags = var.tags
}
```

### main.tf - EKS Addons

```hcl
# modules/eks/main.tf (계속)

# EKS Addons
resource "aws_eks_addon" "vpc_cni" {
  cluster_name = aws_eks_cluster.main.name
  addon_name   = "vpc-cni"

  depends_on = [aws_eks_node_group.main]
}

resource "aws_eks_addon" "coredns" {
  cluster_name = aws_eks_cluster.main.name
  addon_name   = "coredns"

  depends_on = [aws_eks_node_group.main]
}

resource "aws_eks_addon" "kube_proxy" {
  cluster_name = aws_eks_cluster.main.name
  addon_name   = "kube-proxy"

  depends_on = [aws_eks_node_group.main]
}

resource "aws_eks_addon" "ebs_csi_driver" {
  cluster_name = aws_eks_cluster.main.name
  addon_name   = "aws-ebs-csi-driver"

  depends_on = [aws_eks_node_group.main]
}
```

### outputs.tf

```hcl
# modules/eks/outputs.tf
output "cluster_name" {
  value = aws_eks_cluster.main.name
}

output "cluster_endpoint" {
  value = aws_eks_cluster.main.endpoint
}

output "cluster_ca_certificate" {
  value = aws_eks_cluster.main.certificate_authority[0].data
}

output "oidc_provider_arn" {
  value = aws_iam_openid_connect_provider.eks.arn
}

output "oidc_issuer" {
  value = aws_eks_cluster.main.identity[0].oidc[0].issuer
}

output "kubeconfig_command" {
  value = "aws eks update-kubeconfig --name ${aws_eks_cluster.main.name} --region ${data.aws_region.current.name}"
}

data "aws_region" "current" {}
```

---

## 🛠️ Part 3: EKS 배포

### 루트 모듈

```hcl
# main.tf (루트)
module "eks" {
  source = "./modules/eks"

  name            = "${local.name_prefix}-eks"
  cluster_version = "1.28"
  vpc_id          = module.vpc.vpc_id
  subnet_ids      = module.vpc.private_subnet_ids

  node_groups = {
    general = {
      instance_types = ["t3.large"]
      min_size       = 2
      max_size       = 4
      desired_size   = 2
      capacity_type  = "ON_DEMAND"
    }
    spot = {
      instance_types = ["t3.medium", "t3.large"]
      min_size       = 1
      max_size       = 3
      desired_size   = 1
      capacity_type  = "SPOT"
    }
  }

  tags = local.common_tags
}
```

### 배포 및 검증

```bash
terraform apply

# kubeconfig 설정
aws eks update-kubeconfig --name tossplace-dev-eks --region ap-northeast-2

# 클러스터 확인
kubectl get nodes
kubectl get pods -A
```

---

## 🔍 트러블슈팅

### 문제 1: Node 미등록

```bash
# Node가 NotReady인 경우
kubectl describe node <node-name>

# aws-node DaemonSet 확인
kubectl get pods -n kube-system | grep aws-node
```

### 문제 2: OIDC 오류

```bash
# OIDC Provider 확인
aws eks describe-cluster --name <cluster-name> \
  --query 'cluster.identity.oidc.issuer'
```

---

## ✅ 오늘의 체크리스트

| # | 항목 | 완료 |
|---|------|------|
| 1 | EKS 아키텍처 이해 | ☐ |
| 2 | Cluster IAM Role | ☐ |
| 3 | Node Group IAM Role | ☐ |
| 4 | EKS Cluster 생성 | ☐ |
| 5 | Node Group 생성 | ☐ |
| 6 | OIDC Provider 설정 | ☐ |
| 7 | EKS Addons 설치 | ☐ |
| 8 | kubectl 연결 확인 | ☐ |

---

## 🔑 핵심 명령어

```bash
# EKS 관리
aws eks update-kubeconfig --name <name> --region <region>
kubectl get nodes
kubectl get pods -n kube-system

# Node Group 확인
aws eks list-nodegroups --cluster-name <name>
aws eks describe-nodegroup --cluster-name <name> --nodegroup-name <ng>
```

---

## ➡️ 다음 학습: Day 163

**주제**: RDS & 보안 설정

